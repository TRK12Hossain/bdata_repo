# Bangladesh Data Content Platform — Environment Variables
# Copy this file to .env and fill in your actual keys.
# NEVER commit .env to Git. It is already in .gitignore.

# ── Primary LLM: Claude (Anthropic) ─────────────────────────────────────────
# Get from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# ── Fallback LLM: Gemini (Google) ───────────────────────────────────────────
# Get from: https://aistudio.google.com/app/apikey
# Or via Google Cloud: use a service account JSON (see GOOGLE_APPLICATION_CREDENTIALS)
GOOGLE_API_KEY=your_google_gemini_api_key_here

# ── Web Search (Optional — DuckDuckGo is free and needs no key) ─────────────
# Brave Search API is faster and more reliable for research tasks.
# Get from: https://brave.com/search/api/
BRAVE_SEARCH_API_KEY=your_brave_search_api_key_here

# ── Google Workspace (for Sheets / Drive integration) ───────────────────────
# Path to your Google service account JSON key file (never commit this file)
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json
GOOGLE_SHEETS_SPREADSHEET_ID=your_google_sheets_id_here

# ── Pipeline Data Paths ──────────────────────────────────────────────────────
# Where agents write their output files (relative to repo root, or absolute)
PIPELINE_DATA_DIR=/Users/dhrubosmac/bdata_repo/pipeline-data
CONFIG_DIR=/Users/dhrubosmac/bdata_repo/main_repo/config

# ── LLM Model Selection ──────────────────────────────────────────────────────
# Which Claude model to use for reasoning-heavy agents
PRIMARY_MODEL=claude-sonnet-4-6
# Which model to use for fast/cheap tasks
FAST_MODEL=claude-haiku-4-5-20251001
# Gemini fallback model
FALLBACK_MODEL=gemini/gemini-2.0-flash

# ── Slack (for human review notifications) ───────────────────────────────────
# Get from: https://api.slack.com/apps → your app → OAuth Tokens
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token
SLACK_REVIEW_CHANNEL=#content-review
SLACK_ERRORS_CHANNEL=#errors
